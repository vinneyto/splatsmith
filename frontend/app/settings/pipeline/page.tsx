'use client';

export default function PipelineSettingsPage() {
  return (
    <main className="p-6">
      <h1 className="text-2xl font-semibold">Pipeline settings moved</h1>
      <p className="text-sm text-gray-600 mt-2">Pipeline settings are now managed in the dedicated pipeline admin.</p>
    </main>
  );
}
