mode: standalone
api:
  listen_addr: :8080
standalone:
  sqlite_path: ./var/standalone.db
  results_root: ./var/results
  result_url_ttl_seconds: 900
  dev_token: dev-token
  dev_user_id: dev-user
  dev_user_email: dev@example.com
